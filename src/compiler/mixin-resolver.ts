import { RunTimeEnvironment } from '../runtime/context';
import { MixinApplication } from '../model/application';

export class MixinResolver {

  public static loadMixins(mixins: MixinApplication[], runtime: RunTimeEnvironment) {

  }
}